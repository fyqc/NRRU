# 以DMIT为例，谈VPS到手的初步设置

逛V站看到Dmit打折:

```txt
🎉方案名称：DMIT Pro Wee-$39.9 有货啦！ 📅方案配置：1C/1GB/20G SSD/500GB/0.5Gbps 超量限速 2Mbps 无限流量
```

一时心痒，就下单了。

选择了Debian12作为系统，经过近五个小时的等待，开通之后，接下来是一连串的设置，让它变得好用且安全。

## 采用ssh连接VPS

> DMIT 默认禁用密码登录，本身是好事。

我用的是Win11，去Microsoft Store下载安装了Windows Terminal当作我的终端。

### 放到 ~/.ssh

1. 把从VPS那里下载得到的ssh文件（钥匙）解压，里面有三个文件：

```quote
xxx.pem

xxx.ppk

xxx.pub
```

**这三个文件分别是干嘛的**

| 文件     | 用途                                               | 要不要用 |
| ------ | ------------------------------------------------    | ----- |
| `.pem` | **OpenSSH 私钥**（Linux / macOS / Windows Terminal） | ✅ 要用  |
| `.ppk` | **PuTTY 私钥**（PuTTY 专用）                         | ❌ 不用 |
| `.pub` | 公钥（已经在服务器上了）                               | ❌ 不用  |


后，把里面的pem文件放在

```makefile
C:\Users\你的用户名\.ssh\
```

2. 创建/编辑配置文件

```arduino
C:\Users\你的用户名\.ssh\config
```

内容写：

```nginx
Host dmit
    HostName [ip]
    User root
    IdentityFile ~/.ssh/
```

3. 以后直接就可以在terminal里面输入以下命令直接登录VPS：

```bash
ssh dmit
```

## 修改主机名

通过ssh登录之后，看到登录名是root@DMIT-V1Eg97jk1U，感觉不太优雅，这是 DMIT 生成的默认 hostname，完全不可记，以后有多台vps时极容易记混。

改成

```css
root@dmit-la
```

一眼就知道：

* 哪家

* 哪个地区

* 哪个用途

### 直接改 hostname

```bash
hostnamectl set-hostname dmit-la
```

### 确认
```bash
hostname
```

重新登录 SSH，会看到：

```css
root@dmit-la
```

### （可选但推荐）同步 /etc/hosts

打开：
```bash
nano /etc/hosts
```

把原来那行：
```
127.0.1.1 DMIT-V1Eg97jk1U
```

改成：
```
127.0.1.1 dmit-la
```

| 东西                   | 作用位置              |
| -------------------- | ----------------- |
| `Host dmit`          | 本地，用来 `ssh dmit` |
| `hostname = dmit-la` | 服务器内部，用来显示 prompt |

> **改 hostname = 不改变机器本质，只改变你每天看到的名字。**
> 属于“低风险、高幸福感”的改动。

## 防火墙

> DMIT 基本等同于“裸机直连互联网”，默认几乎不帮你挡任何东西

他们的逻辑是：

> **Premium 网络 + 用户自管**

 **安全责任 100% 在你**

### 正确顺序（非常重要）

**顺序错了 = 把自己锁在门外**

1. 安装 UFW
  
```bash
apt update
apt install ufw -y
```

2. 先放行 SSH

现在用的是 22 端口：
```bash
ufw allow 22/tcp
```

3. 设置默认策略

```bash
ufw default deny incoming
ufw default allow outgoing
```

5. 启用防火墙（关键一步）

```bash
ufw enable
```

看到提示：

> Command may disrupt existing ssh connections. Proceed with operation (y|n)?

输入 `y`

**当前 SSH 不会断**

6. 检查状态

```bash
ufw status verbose
```

应该看到类似：
```vbnet
Status: active
To         Action      From
22/tcp     ALLOW       Anywhere
```
